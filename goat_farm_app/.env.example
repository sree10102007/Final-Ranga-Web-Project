# Database Connection Configuration
# Copy this file to '.env' and modify with your actual PostgreSQL connection settings.

# If DATABASE_URL is set, it takes precedence and is used as the connection string
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/goat_farm

DB_HOST=localhost
DB_PORT=5432
DB_NAME=goat_farm
DB_USER=postgres
DB_PASSWORD=changeme
DB_SSLMODE=prefer
SECRET_KEY=generate_with_python_secrets
DB_ENCRYPTION_KEY=your_db_encryption_key_here
FLASK_ENV=production

# Admin Initial Credentials
ADMIN_PASSWORD=changeme

# Rate Limiter Backend Store (use redis://redis:6379/0 in production docker setup).
# WARNING: memory:// must NEVER be used with more than one Gunicorn worker as rate limit counters won't be shared.
LIMITER_STORAGE_URI=redis://redis:6379/0
